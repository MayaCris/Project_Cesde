package org.torneo.modelos.hijos;

public interface Entrenador {
    //Interface es una clase que no tiene cuerpo, solo tiene metodos (por defecto abstractos)
    // para implementar en otras clases

     void mejorarDefensa();
     void mejorarAtaque();
     void meditar();
}
